<%= form_for(current_user.class_bookings.build(exercise_class_id: @exerciseclass.id)) do |f| %>
  <div><%= f.hidden_field :exercise_class_id %></div>
  <%= f.submit "Attend", class: "btn btn-large btn-primary" %>
<% end %>