<%= form_for(current_user.class_bookings.find_by(exercise_class_id: @exerciseclass.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Cancel", class: "btn btn-large" %>
<% end %>